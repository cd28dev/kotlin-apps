package com.example.listaimagenes.presentation.theme

import androidx.compose.ui.graphics.Color

object ColoresApp {

    // 🎨 Colores principales
    val Primario = Color(0xFF1565C0)      // Azul elegante
    val Secundario = Color(0xFF00BCD4)    // Aqua moderno
    val Terciario = Color(0xFF8E24AA)     // Morado vibrante
    val Confirmacion = Color(0xFF43A047)  // Verde éxito
    val Error = Color(0xFFE53935)         // Rojo Material

    // 🎨 Fondos
    val FondoClaro = Color(0xFFF9FAFB)    // Gris muy claro (no blanco puro)
    val FondoOscuro = Color(0xFF121212)   // Dark estándar

    // 🎨 Superficies
    val SuperficieClaro = Color(0xFFFFFFFF) // Blanco puro para tarjetas/dialogs
    val SuperficieOscura = Color(0xFF1E1E1E) // Gris carbón para superficies

    // 🎨 Texto
    val TextoPrincipal = Color(0xFF212121)   // Gris muy oscuro
    val TextoSecundario = Color(0xFF616161)  // Gris medio (para subtítulos)
    val TextoInverso = Color(0xFFFFFFFF)     // Blanco para fondos oscuros

    // 🎨 Extras
    val Outline = Color(0xFFBDBDBD)          // Bordes neutros
    val OutlineVariant = Color(0xFFE0E0E0)   // Versión más clara
    val Scrim = Color(0x66000000)            // Negro translúcido para overlays
}

